# Core numerical / ML stack (PyCharm 2025.1, Python 3.10 - 3.12 recommended)
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.8

# Deep learning (CPU wheels are fine; install the CUDA build for --scale full)
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.1

# Optional: pretty console progress for the long 'full' runs
tqdm>=4.66
